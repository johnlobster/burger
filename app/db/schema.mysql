-- mysql schema for burger app
-- John  Webster

DROP DATABASE IF EXISTS burger_db;
CREATE DATABASE burger_db;
USE burger_db;

CREATE TABLE burger (
    id INT NOT NULL AUTO_INCREMENT,
    burger_name VARCHAR(60) NOT NULL DEFAULT "",
    devoured BOOLEAN NOT NULL DEFAULT false,
    PRIMARY KEY (id)
);